#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int n;
    scanf("%d",&n);
    int r=0;
    int t=0;
    for(int i=0;i<n;i++){
        r=2+(r*10);
        t=t+r;
    }
    printf("%d",t);
    return 0;
}
